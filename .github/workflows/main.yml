   name: Deploy to Render

   on:
     push:
       branches:
         - main

   jobs:
     test-and-deploy:
       runs-on: ubuntu-latest

       steps:
       - name: Validacion previa
         run: |
           date
           hostname
           pwd
           ls -l
           find . 

